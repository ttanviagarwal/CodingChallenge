import { StyleSheet } from 'react-native';
import {GRAY_COLOR} from './CommonColors';

/**
 * Common styles applies here.
 */
export default StyleSheet.create({
    menu: { justifyContent:'flex-end', flexDirection:'row', padding:10, borderBottomWidth:1, borderColor:GRAY_COLOR}
});